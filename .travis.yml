language: ruby
rvm:
  - 2.7.1
cache: bundler
dist: trusty
services:
  - postgresql
addons:
  postgresql: "10.14"
before_script:
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database astronomaniaapi_test;' -U postgres
